// check that data creators are:
//  setup and building needed objects
//  are validating data
import * as atomic from '../data/atomicData'


describe("agency reducer tests", () => {

  describe("loads set of objects", () => {

    test("with valid data, returns new state object containing payload", () => {})
    test("with invalid data, returns previous state", () => {})
    test("state object contains valid structure", () => {})
    test("", () => {})

  })

  describe("create, update, delete agency objects", () => {
    test("", () => {})


  })
  describe("", () => {
    test("", () => {})
  })


})
